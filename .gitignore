# Dependencies (only needed for the optional dev tooling in scripts/)
node_modules/
package-lock.json

# Editor / OS noise
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Local env / secrets — never commit Apps Script deployment ids or keys
.env
.env.local
.clasp.json
